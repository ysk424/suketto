# 助人の開発・公開方針

- 表示名は「助人」、拡張機能の識別子は `suketto`。
- 画面の説明、文書、コミットメッセージは日本語とする。製品名、コード、API 識別子は原表記を使う。
- 制作は塚本吉彦、2026年日本制作。著作権表記は README と LICENSE、マニフェストで一致させる。
- 公開ライセンスの識別子は `LicenseRef-All-Rights-Reserved`。変更は利用者の明示的な指示に従う。
- 認証情報、個人の会話履歴、作業中のブレンドファイルをリポジトリに含めない。
- Blender のデータ操作はメインスレッドで行う。通信スレッドから `bpy` を呼ばない。
- 問い合わせは Terra / medium、通常操作は Astra / medium、難しい仕事は Astra / xhigh を維持する。
- 実シーンの検証では利用者の開いている作品や既存ファイルを上書きしない。
- 実際の Codex 接続を使う検証は利用枠を消費する。表示や文書だけの変更で繰り返さない。
